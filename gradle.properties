kotlin.code.style=official
# The repo's own package.json pins Yarn 4 (corepack), which breaks the Kotlin/JS
# plugin's Yarn-1 invocation; use npm for the (dependency-free) JS toolchain instead.
kotlin.js.yarn=false
org.gradle.jvmargs=-XX:MaxMetaspaceSize=512m
